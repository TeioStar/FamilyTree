## 1. 项目骨架与配置

- [ ] 1.1 定义家谱目录配置项（例如 `FAMILY_DB_DIR`）并在启动流程中读取
- [ ] 1.2 建立 SQLite 连接工厂并统一启用 `WAL` 与 `busy_timeout`
- [ ] 1.3 建立每库迁移入口与 `schema_version` 检查流程

## 2. 家谱文件扫描与注册（family-file-registry）

- [ ] 2.1 实现启动时扫描 `*.ftree.db` 文件并读取 `meta` 元数据
- [ ] 2.2 实现 FamilyRegistry 内存索引（`family_id -> dbPath/status`）
- [ ] 2.3 实现损坏库/元数据缺失库的隔离与错误记录
- [ ] 2.4 实现家谱列表查询接口，返回可用状态与错误摘要

## 3. 核心数据管理（genealogy-data-management）

- [ ] 3.1 创建人物、关系、事件、附件及审计日志的数据表与约束
- [ ] 3.2 实现按 `family_id` 选库读写的服务层封装，禁止跨库访问
- [ ] 3.3 实现人物与事件新增/更新接口及输入校验
- [ ] 3.4 实现关系创建校验（禁止自关联与重复主关系）
- [ ] 3.5 实现删除操作的权限校验与审计落库

## 4. 权限控制（family-rbac）

- [ ] 4.1 实现 `creator/admin/member` 角色枚举与权限矩阵
- [ ] 4.2 实现成员关系表及角色变更服务
- [ ] 4.3 实现“至少一个 creator”与“admin 不可操作 creator”约束
- [ ] 4.4 实现高风险操作二次确认机制与越权审计记录

## 5. 树查询与可视化契约（tree-query-and-visualization）

- [ ] 5.1 实现树查询接口参数（`root_person_id`、`depth`）与校验
- [ ] 5.2 实现按层加载查询，返回节点边结构（`nodes`/`edges`）
- [ ] 5.3 实现截断续取标记（`has_more` 或游标）与分支状态
- [ ] 5.4 实现同级节点稳定排序，确保重复请求顺序一致

## 6. 质量保障与交付

- [ ] 6.1 为扫描注册、RBAC、关系校验、树查询补充单元测试
- [ ] 6.2 为关键 API 补充集成测试（含越权与异常库场景）
- [ ] 6.3 编写运维文档：家谱文件放置规范、备份与恢复流程
- [ ] 6.4 编写回滚与故障处理手册（单库不可用不影响全局）
